<%- include('./partials/head', {title: 'List Shipment' }) %>
  
  <a href="/shipments/<%= user_id %>/add">Add new shipment</a>

  <table class="table">
    <thead class="table-dark">
      <tr>
        <th>Shipment ID</th>
        <th>Shipment Created Date</th>
        <th>current_latitude</th>
        <th>current_longitude</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      
      <% shipments.forEach(shipment => { %>
        <% let {id, createdAt, current_latitude, current_longitude} = shipment %>

        <tr>
          <td><%= id %></td>
          <td><%= createdAt %></td>
          <td><%= current_latitude %></td>
          <td><%= current_longitude %></td>
          <td>
            <a href="/destinations/<%= id %>/list"><button>view destinations</button></a>
            |
            <a href="/destinations/<%= id %>/add"><button>add destination</button></a>
          </td>
        </tr>

        <% }) %>
      
    </tbody>
  </table>


  <%- include('./partials/foot') %>